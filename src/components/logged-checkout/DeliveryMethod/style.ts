import { StyleSheet } from "react-native";
import { fontFamilies, colors, fontSizes } from "../../../styles/globalStyles";

export const styles = StyleSheet.create({
  title: {
    zIndex: 0,
    fontFamily: fontFamilies.extraBoldOpenSans,
    color: colors.black,
    fontSize: fontSizes.m,
  },
});
